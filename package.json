{
  "name": "conciliacion-bancaria",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicación de conciliación bancaria automática",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "predev": "npx kill-port 3001",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build && npm --prefix server run build && node scripts/prepare-vercel-api.cjs",
    "start": "node server/dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "kill-port": "^2.0.1"
  }
}
